apiVersion: apps/v1
kind: Deployment
metadata:
  name: crypto-insight
  namespace: crypto-insight
  labels:
    app: crypto-insight
spec:
  replicas: 3
  selector:
    matchLabels:
      app: crypto-insight
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: crypto-insight
    spec:
      containers:
        - name: crypto-insight
          image: us-central1-docker.pkg.dev/crypto-insight-k8s/crypto-insight/crypto-insight:94f3ea2a36a29ff07e52aca082bd7c4cb8042774
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: crypto-insight-config
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
      restartPolicy: Always